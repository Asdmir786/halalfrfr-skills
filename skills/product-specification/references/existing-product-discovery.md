# Existing Product Discovery
Inspect routes, navigation, APIs/actions, state transitions, forms, permissions, errors, jobs, integrations, and lifecycle. Treat current behavior as evidence, not automatically as desired future behavior. Separate product contract from implementation debt and record hidden dependencies a rewrite could lose.
