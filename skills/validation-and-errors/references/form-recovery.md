# Form Recovery
Preserve safe user input, attach field errors correctly, surface business errors near the operation, prevent duplicate submits, handle conflict/stale states, distinguish retryable transport failures, and preserve accessible focus/announcement behavior.
