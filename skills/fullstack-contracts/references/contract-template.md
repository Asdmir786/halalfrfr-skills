# Contract Template
Record operation/purpose, callers, transport, authentication, authorization, input, output, stable errors, side effects, compatibility, and verification. Keep contract language independent of specific UI components and backend controller names.
