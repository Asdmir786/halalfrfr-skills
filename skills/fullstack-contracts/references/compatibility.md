# Compatibility
Compatible changes preserve documented caller assumptions. Potentially breaking changes include required fields, types/nullability, auth, error semantics, ordering, pagination, side effects, and state transitions. Prefer additive evolution when independently deployed/external consumers exist.
