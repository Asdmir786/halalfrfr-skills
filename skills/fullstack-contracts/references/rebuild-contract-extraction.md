# Rebuild Contract Extraction
For each critical flow trace UI entry → actual request/action → input construction → auth/session → response → errors/loading/empty → side effects/redirects → shared types/schemas → backend implementation. The old frontend is evidence, not the future architecture.
