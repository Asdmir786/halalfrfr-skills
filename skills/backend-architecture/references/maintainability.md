# Backend Maintainability
Prefer cohesive modules, explicit dependencies, small public surfaces, stable contracts, local reasoning, framework-native lifecycle, and meaningful boundary tests. Avoid global mutable state, duplicated rules, implicit side effects, huge utility modules, generic abstraction layers, and premature microservices.
