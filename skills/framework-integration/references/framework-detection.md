# Framework Detection
Use package/composer manifests, lockfiles, configs, framework directories, scripts, deployment/runtime files, and code idioms. When version-specific behavior affects correctness, verify the installed version and relevant official documentation rather than relying on memory.
