# Boundary Design
Ask who owns the rule, which layer validates authoritatively, what contract stays stable when UI/persistence changes, and whether an abstraction has real responsibility. Prefer explicit data flow, narrow dependency direction, and framework-native structure over ceremony.
