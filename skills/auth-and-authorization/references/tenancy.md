# Tenancy
Define tenant identity and membership, resource ownership, authoritative read/write scoping, cross-tenant denial, and explicit privileged exceptions. Never trust client tenant IDs without membership/permission verification.
