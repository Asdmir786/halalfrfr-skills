# Auth Audit
Check login/logout/session renewal, protected routes, identity source, permission enforcement, ownership, tenant scoping, admin escalation, recovery/provider flows if present, service/webhook auth, stale sessions, and allowed/denied tests.
